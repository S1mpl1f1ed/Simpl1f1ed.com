{
  "name": "simpl1f1ed.com",
  "version": "v0.1",
  "private": true,
  "dependencies": {
    "axios": "^1.4.0",
    "firebase": "^10.1.0",
    "react": "^18.2.0",
    "react-firebase-hooks": "^5.1.1",
    "react-router-dom": "^7.1.0",
    "react-scripts": "5.0.1",
    "sass": "^1.63.6"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "emulate": "npm i && npm run build && firebase emulators:start",
    "deploy": "npm i && npm run build && firebase deploy"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@babel/plugin-proposal-private-property-in-object": "^7.21.11",
    "@types/react": "^18.2.15"
  }
}
